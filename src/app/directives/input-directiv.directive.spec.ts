import { InputDirectivDirective } from './input-directiv.directive';

describe('InputDirectivDirective', () => {
  it('should create an instance', () => {
    const directive = new InputDirectivDirective();
    expect(directive).toBeTruthy();
  });
});
